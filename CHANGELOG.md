# Changelog

## v1.0.0

- 完成 APK UI 拆解与重设计主流程：导入 APK、框架识别、资源提取、WebView/H5 原页面运行、DOM 自动拆层、滚动页连续捕获、CSS 背景图与伪元素识别。
- 支持把当前原页面截图作为半透明底稿继续重绘。
- 捕获后自动生成可编辑页面，并继续使用现有图层、文字、按钮、图片、面板、拖动、缩放、裁剪、样式、组件、模板与多比例适配工具。
- 加入工程自动备份、最近备份恢复、工程 ZIP 导入恢复。
- 加入普通 PNG 与透明 PNG 页面导出。
- 加入游戏接入包导出：ui-layout.json + 当前使用资源。
- 保存改为临时文件替换，降低异常退出导致 project.json 损坏的风险。
- 完成当前阶段全部计划中的手机端 UI 编辑、APK 拆解、重建设计与导出闭环。


## v0.10.0

- 针对 city-restaurant-game-v2 这类 WebView/H5 APK，加入“运行原页面并自动拆层”。
- APK 拆解后可直接在内置 WebView 中运行 assets/index.html，并正常加载同目录 CSS、JS 与图片资源。
- 允许在原页面预览中直接点击、切页、进入菜单，再捕获当前正在显示的界面，而不是只解析静态 index.html。
- 新增 DOM 运行时捕获：读取可见元素真实位置、尺寸、文字、颜色、字号、字重、圆角、边框、透明度、层级、图片地址与背景图。
- 捕获结果自动换算到 1080×1920 母版坐标。
- 自动把可识别节点转换为编辑器的文字、按钮、面板和图片图层。
- 原 APK 中对应图片会自动复制进当前工程素材库并绑定到新图层。
- 捕获完成后自动新建“自动拆页”页面，立即进入现有拖拽、缩放、属性、适配和图层编辑流程。
- Web 预览默认阻止网络请求，只运行 APK 内已提取的本地资源，降低未知 APK 访问外部网络的风险。
- DOM 捕获限制最大节点数量，并过滤不可见、极小、脚本和样式节点，避免复杂页面把编辑器拖死。


## v0.9.0

- 新增 APK UI 拆解入口，可直接从手机选择 APK。
- 新增 APK 安全扫描与框架识别：WebView/H5、原生 Android、Unity、Godot、未知/混合。
- 可提取 APK 内图片、字体、XML、HTML/CSS/JS/JSON/SVG 等可用资源。
- 针对 city-restaurant-game-v2 这类 WebView APK，识别 assets/index.html、game.css、game.js 与 ui-v2 资源目录。
- 拆解结果显示包名、版本、框架、条目数及各类资源统计。
- 提取出的图片可一键加入 Game UI Studio 全局素材库。
- 可直接从拆解结果新建“APK 重建设计页”，继续用现有竖屏画布重做 UI。
- 加入 Zip Slip 路径防护、单资源大小限制、总解包大小限制与最大条目限制，避免异常 APK 把编辑器拖死。
- 第一阶段不伪装成源码还原：原生编译 XML、Unity Prefab/Atlas、Godot PCK 深度解析保留给后续版本。


## v0.8.0

- 进入布局效率专项。
- 新增复制样式 / 粘贴样式：可把字体、颜色、圆角、边框、阴影、透明度、图片显示方式等快速复用到其他元素。
- 多选新增同宽 / 同高 / 同尺寸。
- 新增批量尺寸编辑，可统一修改多个元素的宽度和高度。
- 吸附参考线新增“吸附距离”数字提示，拖动时更容易判断当前位置。
- 图层面板支持长按后上下拖动排序；背景图层固定在底部，不参与普通拖动排序。
- 图层面板增加背景标识。
- 组件保存时可直接命名，已有组件支持改名。
- 页面模板保存时可直接命名，已有模板支持改名。
- 组件和页面模板增加缩略布局预览，不再只显示文字列表。
- 保留 v0.7.0 的数值属性、文字样式、颜色、渐变、边框和阴影编辑。


## v0.7.0

- 进入 UI 设计能力专项，属性面板新增精确 X / Y / 宽 / 高数值编辑。
- 文字与按钮支持直接编辑内容、字号、字重、字体类型和左/中/右对齐。
- 新增文字颜色、填充颜色、边框颜色和渐变终点颜色编辑。
- 颜色支持 #RRGGBB / #AARRGGBB 输入，并提供常用颜色快捷色板。
- 按钮与面板支持渐变填充开关。
- 按钮与面板新增边框粗细、圆角、透明度、阴影强度、阴影大小和阴影偏移。
- 新增无衬线 / 衬线 / 等宽三类系统字体。
- 文本元素新增独立透明度控制。
- 样式滑杆改为单次事务记录，避免拖动滑杆时污染 100 步撤销历史。
- 工程格式升级为 version 6，旧工程继续兼容读取并使用默认样式字段。


## v0.6.0

- 进入触屏操作专项，底部新增“画布”模式入口，不再把画布平移/缩放藏在更多菜单里。
- 画布模式支持拖动画布、双指缩放，双击可快速恢复适配视图。
- 选中单个元素时改为四角缩放控制点，触摸目标扩大到 32dp，手机上更容易抓住。
- 图片元素四角缩放默认保持比例；背景元素不显示缩放控制点。
- 新增长按元素快捷菜单：裁剪、属性/适配、复制、置顶、置底、锁定、删除。
- 裁剪模式增加明显状态提示，提示单指拖图、双指缩放/旋转。
- 裁剪工具条新增重置、左转、右转快捷操作。
- 新增精细移动按钮，可按 4 设计单位向上下左右微调元素。
- 吸附阈值从 12 提高到 18 设计单位，降低手机拖动时“差一点吸不上”的问题。
- 保留 v0.5.0 的 9:16 母版、多比例预览、安全区和一键适配检查。


## v0.5.0

- 进入“适配专项”，9:16 继续作为母版设计坐标，不再等同于最终设备尺寸。
- 新增 9:19.5 主流长屏预览，并保留 9:20、9:21、10:16、平板竖屏。
- 新增 9:16 核心区可视化：长屏多出的区域用橙色边界标出，核心按钮/文字可保持在核心区。
- 系统安全区改为独立绿色边界，用于提醒刘海、挖孔、圆角与底部手势区域。
- 新增“一键适配检查”：自动扫描所有内置比例，检查元素越界、按钮明显重叠、安全区冲突、长屏核心区外元素和图片异常拉伸。
- 新增自定义设备预览，可输入任意竖屏分辨率。
- 新增背景专用模式：背景元素自动覆盖当前预览屏幕，不受普通 UI 锚点规则限制。
- 图片新增铺满裁剪 / 完整显示 / 拉伸 / 适应宽 / 适应高五种显示方式。
- 适配检查会针对背景使用完整显示或拉伸给出风险提示。
- 工程格式升级为 version 5，旧工程继续兼容读取。


## v0.4.2

- 编辑器改为竖屏优先，应用方向切换为 sensorPortrait。
- 逻辑设计画布从 1920×1080 改为 1080×1920。
- 默认预览改为 9:16，并加入 9:20、9:21、10:16、平板竖屏。
- 旧横屏工程首次打开时自动迁移到竖屏坐标系，素材不丢失。
- 竖屏手机界面重排：顶部只保留项目、撤销/重做、比例、适配和更多。
- 底部常驻 6 个主要入口：图片、文字、按钮、页面、图层、更多。
- 去掉左右两条工具栏，尽量把手机宽度留给竖屏画布。
- 选中元素时，上方仅出现上下文编辑条；未选中时画布获得最大空间。
- “更多”中收纳面板、素材库、安全区、吸附、多选、画布手势、保存与导出。


## v0.4.1

- 重做横屏手机编辑器布局，移除占据大量高度的常驻页面栏、素材栏和底部工具栏。
- 顶部改为 48dp 紧凑工具条，仅保留工程名、撤销/重做、适配预览、缩放和更多。
- 左侧加入常用工具栏：图片、文字、按钮、面板、多选、画布。
- 右侧加入快捷入口：图层、页面、素材、属性。
- 仅在选中元素时显示底部上下文工具条，让画布获得更多可用空间。
- 增加沉浸式横屏，系统状态栏/导航栏默认隐藏，可从边缘临时呼出。
- 项目设置、吸附、安全区、工程导出收纳到“更多”面板。


## 0.4.0 - 2026-09-22

- 新增工程级全局素材库，导入图片自动登记为可跨页面复用的素材。
- 新增资源库面板，可从素材库把已导入图片再次放入当前画布。
- 新增组件系统，可把当前选择保存为组件并重复插入。
- 组件实例自动重建元素 ID，并以新的 groupId 作为独立组合插入。
- 新增页面模板，可保存当前页并从模板一键生成新页面。
- 页面模板实例会重建元素 ID 与分组 ID，避免跨页面冲突。
- 新增组内编辑模式；普通状态整组选中，组内编辑状态可单独操作成员。
- 新增“画布手势”导航模式，暂停元素编辑后可自由拖动画布并双指缩放。
- 工程格式升级为 version 3，新增 assetLibrary / components / pageTemplates。
- 完整工程 ZIP 会同时收集全局素材库以及组件、页面模板引用的图片。
- 保持对 v0.1/v0.2 单页面结构和 v0.3 pages 结构的读取兼容。
- Android versionCode 升级为 4，versionName 升级为 0.4.0。

## 0.3.0 - 2026-09-22

- 新增多选模式，可点击追加/取消选择。
- 新增画布拖框框选与多选包围框。
- 新增多元素整体移动。
- 新增多元素对齐：左/中/右、顶/中/底。
- 新增横向等间距与纵向等间距。
- 新增组合 / 取消组合；组合元素支持整体选择与移动。
- 新增多页面数据模型与页面管理面板。
- 新增页面新建、复制、重命名、删除、切换。
- 新增底部快速页面切换栏。
- 工程格式升级为 version 2 / pages 结构，并兼容自动迁移 v0.1、v0.2 的单页面 elements。
- 工程 ZIP 导出会收集所有页面实际使用的素材。
- Android versionCode 升级为 3，versionName 升级为 0.3.0。

## 0.2.0 - 2026-09-22

- 新增正式图层面板：选择、隐藏/显示、锁定/解锁、上移/下移。
- 新增自动吸附系统，可吸附画布边缘、中心线以及其他元素的边缘/中心。
- 新增实时粉色参考线，拖动元素时显示当前吸附位置。
- 新增吸附开关，支持临时关闭。
- 新增“快速对齐画布”：左、水平中、右、顶、垂直中、底。
- 保留并继续兼容 0.1.0 的锚点、响应式适配、裁剪、图片调节、撤销/重做与工程导出。
- Android versionCode 升级为 2，versionName 升级为 0.2.0。

## 0.1.0 - 2026-09-22

### Added
- 建立 Android 原生 Kotlin + Jetpack Compose 项目。
- 建立 1920×1080 逻辑画布与多设备预览系统。
- 建立 UI 元素数据模型、锚点和尺寸适配规则。
- 加入图片、文字、按钮、面板。
- 加入图片导入、本地素材目录和非 Base64 工程存储。
- 加入元素拖动、缩放、复制、删除、置顶、置底和锁定。
- 加入图片裁剪缩放、旋转、翻转及基础图像调整。
- 加入撤销 / 重做历史框架。
- 加入自动保存、JSON 导出和完整工程 ZIP 导出。
- 加入 GitHub Actions APK 构建工作流。
